@model IList<ECommerce.Shared.CategoryVm>
<head>
    <link rel="stylesheet" href="~/css/StyleSheet.css" />
</head>

<ul class="navbar-nav menu01" style="margin: 0 auto;">
    @foreach (var cat in Model)
    {
    <li class="nav-item" style="float:left;">
        @*<a class="nav-link" style="text-align:center;" href="/categories/@cat.CategoryId">@cat.CategoryName</a>*@
        <a class="nav-link active" style="font-size:20px" asp-controller="Product" asp-action="GetProductByCategory" asp-route-idCate="@cat.CategoryId">@cat.CategoryName</a>
    </li>
    }
    
</ul>
